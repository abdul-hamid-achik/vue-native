---
"@thelacanians/vue-native-runtime": patch
---

Prevent Android modal teardown from dispatching dismiss events, and publish Android source archives without duplicate Kotlin entries.

Enforce warning-free iOS Swift linting and move the remaining GitHub Actions to Node 24 runtimes.
