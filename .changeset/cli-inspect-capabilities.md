---
"@thelacanians/vue-native-cli": minor
---

Add `vue-native inspect [--json]` (project snapshot) and `vue-native capabilities [--json]` (framework component/module/limitation manifest). VSectionList `stickySectionHeaders` now applies at runtime on iOS and Android, matching macOS.
